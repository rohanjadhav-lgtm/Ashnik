# Connectors

This directory contains global connector (datasource) configurations.

Connectors define connections to external data sources like databases, APIs, and cloud services.

Agents can reference these connectors and optionally override specific settings.
